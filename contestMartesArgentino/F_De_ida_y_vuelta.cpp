#include <bits/stdc++.h>
#define ll long long
#define endl '\n'
using namespace std;

void solve(){
    int c;

    while (cin >> c){
        string p; cin >> p;
        vector<vector<char>> mp(p.size()/c);

        for(int i = 0; i < mp.size(); i++){
            for(int j = 0; j < mp[i].size(); j++){
                mp[i][j] = 
            }
        }



    }
}

int main()
{
    ios::sync_with_stdio(false); cin.tie(nullptr);
    int T = 1;
    while(T--){solve();}
    return 0;
}
