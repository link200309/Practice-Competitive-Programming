#include <bits/stdc++.h>
using namespace std;


int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    int n, q;

    cin >> n >> q;
    vector<int> ve(n);

    for(int &x : ve) cin >> x;

    int x;
    while(q--){
        cin >> x;
        

    }

    return 0;
}
